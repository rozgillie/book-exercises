# Send the HTTP Request to download the data
# Extract the content and convert it from JSON
get <- GET(paste0(base, endpoint), query = key_param)
# Use `source()` to load your API key variable from the `apikey.R` file you made.
# Make sure you've set your working directory!
source("apikey.R")
library("jsonlite")
# load relevant libraries
library("httr")
# Create a variable `movie_name` that is the name of a movie of your choice.
movie_name <- "Parasite"
key_param <- list(
"api-key" = nyt_key,
"movie_name" = movie_name
)
# Construct an HTTP request to search for reviews for the given movie.
base <-"https://api.nytimes.com/svc/movies/v2/"
# Send the HTTP Request to download the data
# Extract the content and convert it from JSON
get <- GET(paste0(base, endpoint), query = key_param)
# Construct an HTTP request to search for reviews for the given movie.
base <-"https://api.nytimes.com/svc/movies/v2/"
endpoint <- "reviews/search.json"
# Send the HTTP Request to download the data
# Extract the content and convert it from JSON
get <- GET(paste0(base, endpoint), query = key_param)
body <- content(get, "text")
body <- content(get, "text")
body <- fromJSON(content(get, "text"))
# Send the HTTP Request to download the data
# Extract the content and convert it from JSON
get <- GET(paste0(base, endpoint), query = key_param)
body <- fromJSON(content(get, "text"))
body <- fromJSON(content(get, "text"))
# Use `source()` to load your API key variable from the `apikey.R` file you made.
# Make sure you've set your working directory!
source("apikey.R")
# Send the HTTP Request to download the data
# Extract the content and convert it from JSON
get <- GET(paste0(base, endpoint), query = key_param)
body <- fromJSON(content(get, "text"))
base_url <- "https://api.themoviedb.org/3"
# runs apikey file to use variable defined in that file
source("apikey.R")
# list of URL parameters containing api key
key_param <- list(
api_key = tmdb_key
)
# runs apikey file to use variable defined in that file
source("apikey.R")
# runs apikey file to use variable defined in that file
print(source("apikey.R"))
# list of URL parameters containing api key
key_param <- list(
api_key = "TieWrcIAGGvYhYaPtXDuBEf5xkppzHTj9"
)
# returns data frame with top 5 currently trending actors, and with the info formatted for R markdown
get <- GET(paste0(base_url, "/trending/person/week"), query = key_param)
body_content <- fromJSON(content(get, "text"))
is.data.frame(body_content)
is.list(body_content)
# load relevant libraries
library("httr")
library("jsonlite")
# Use `source()` to load your API key variable from the `apikey.R` file you made.
# Make sure you've set your working directory!
source("apikey.R")
# Create a variable `movie_name` that is the name of a movie of your choice.
movie_name <- "Parasite"
key_param <- list(
"api-key" = nyt_key,
"movie_name" = movie_name
)
# Construct an HTTP request to search for reviews for the given movie.
base <-"https://api.nytimes.com/svc/movies/v2/"
endpoint <- "reviews/search.json"
# Send the HTTP Request to download the data
# Extract the content and convert it from JSON
get <- GET(paste0(base, endpoint), query = key_param)
print(get)
# Use `source()` to load your API key variable from the `apikey.R` file you made.
# Make sure you've set your working directory!
source("apikey.R")
print(body)
